# Copy this file to `.env.local` for local development.
# Add only the variables your app actually uses.

# Public variables (available in the browser)
# NEXT_PUBLIC_SITE_URL=https://example.com

# Contact form delivery (server-only)
# Option A (recommended): Telegram bot delivery
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Option B: Generic webhook (if set, this is used instead of Telegram)
# CONTACT_WEBHOOK_URL=https://example.com/webhook/contact

# Anti-spam / rate limiting (optional overrides)
# CONTACT_RATE_LIMIT_MAX=5
# CONTACT_RATE_LIMIT_WINDOW_MS=600000
# CONTACT_MIN_SUBMIT_MS=1200
